---
phase: 04-cross-platform-gui
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - internal/gui/screens/settings.go
  - internal/gui/screens/main.go
  - internal/gui/app.go
autonomous: true
requirements:
  - GUI-05
  - GUI-07
  - GUI-10

must_haves:
  truths:
    - "User can access a Settings screen from the main view"
    - "Settings screen exposes gateway URL, verbose mode, game directory, and Wine path (Linux) options"
    - "Changes in Settings are persisted to the config file"
    - "GUI runs fullscreen on Steam Deck, windowed on desktop"
    - "All CLI subcommands continue working unchanged when invoked directly"
    - "Bot name field is present in main view (placeholder if API unavailable)"
  artifacts:
    - path: "internal/gui/screens/settings.go"
      provides: "Settings screen with config options"
      contains: "func MakeSettingsView"
    - path: "internal/gui/screens/main.go"
      provides: "Updated main view with bot name field and settings navigation"
      contains: "botNameEntry"
    - path: "internal/gui/app.go"
      provides: "Updated app with settings navigation and Steam Deck fullscreen"
      contains: "showSettingsView"
  key_links:
    - from: "internal/gui/screens/settings.go"
      to: "internal/config/config.go"
      via: "Settings reads/writes config values"
      pattern: "config\\.Config"
    - from: "internal/gui/screens/main.go"
      to: "internal/gui/screens/settings.go"
      via: "Settings button navigates to settings view"
      pattern: "showSettingsView"
    - from: "internal/gui/app.go"
      to: "fyne.Window.SetFullScreen"
      via: "Steam Deck detection triggers fullscreen"
      pattern: "SetFullScreen"
---

<objective>
Implement the Settings screen, bot name field in main view, Steam Deck fullscreen detection, and ensure CLI subcommands remain unchanged. Settings exposes key config options. Bot name field is included per the Windows launcher feature parity requirement.

Purpose: Completes the feature parity with the Windows launcher (settings, bot name) and ensures Steam Deck compatibility (fullscreen mode). CLI preservation is a locked decision.
Output: Settings screen, bot name in main view, Steam Deck fullscreen, verified CLI compatibility.
</objective>

<execution_context>
@/home/cstory/.claude/get-shit-done/workflows/execute-plan.md
@/home/cstory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-cross-platform-gui/04-CONTEXT.md
@.planning/phases/04-cross-platform-gui/04-RESEARCH.md
@.planning/phases/04-cross-platform-gui/04-02-SUMMARY.md
@internal/config/config.go
@internal/gui/app.go
@internal/gui/screens/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Settings screen with config persistence</name>
  <files>
    internal/gui/screens/settings.go
  </files>
  <action>
1. Create `internal/gui/screens/settings.go` with `//go:build gui` tag:
   - Package `screens`
   - `func MakeSettingsView(w fyne.Window, cfg *config.Config, onBack func()) fyne.CanvasObject`
   - Layout:
     a. Title: "Settings" (bold, larger text)
     b. Form section using Fyne's `widget.Form` or manual VBox layout:
        - **Gateway URL**: `widget.NewEntry()` pre-filled with `cfg.Gateway`, placeholder "https://gateway-dev.project-crown.com"
        - **Verbose Mode**: `widget.NewCheck("Enable verbose output", nil)` pre-set to `cfg.Verbose`
        - **Game Directory**: `widget.NewEntry()` pre-filled with `cfg.GameDir`, placeholder showing default path. Optionally add a folder browse button using `dialog.ShowFolderOpen()` if Fyne supports it, otherwise just the text entry.
        - **Wine Path** (Linux only): `widget.NewEntry()` pre-filled with `cfg.WinePath`, placeholder "auto-detect". Only show this on Linux (use `runtime.GOOS == "linux"` check).
        - **Wine Prefix** (Linux only): `widget.NewEntry()` pre-filled with `cfg.WinePrefix`, placeholder showing default path. Only show on Linux.
        - **Host X** (game server): `widget.NewEntry()` pre-filled with `cfg.HostX`, placeholder "157.90.131.105"
     c. Buttons row:
        - "Save" button (`widget.HighImportance`): validates entries, writes config to disk via viper, updates `cfg` struct in memory
        - "Back" button: calls `onBack()` to return to main view (discard unsaved changes)
   - Save logic:
     - Update viper values: `viper.Set("gateway", gatewayEntry.Text)`, etc.
     - Write config file: `viper.WriteConfigAs(config.ConfigFile())` (create if not exists, update if exists)
     - Update the in-memory cfg struct to reflect changes
     - Show success dialog: `dialog.ShowInformation("Settings Saved", "Configuration updated.", w)`
   - Use `widget.NewForm()` for clean label-field pairs, or build manually with `container.NewGridWithColumns(2, label, entry)` rows
   - Handle the case where config file doesn't exist yet (first save creates it)

2. Keep discretion choices reasonable:
   - Only expose the most useful settings (gateway, verbose, game dir, wine path/prefix, hostx)
   - Leave advanced settings (like WINEFSYNC, WINEDLLOVERRIDES) to the TOML config file -- do not expose in GUI per Claude's discretion
  </action>
  <verify>
1. `go build -tags gui -o /tmp/cluckers-gui ./cmd/cluckers` succeeds
2. `go vet -tags gui ./...` passes
3. GUI Settings screen shows all form fields pre-populated with current config values
4. Changing a value and clicking Save persists to config file
5. Back button returns to main view without saving changes
  </verify>
  <done>
- Settings screen shows gateway URL, verbose toggle, game directory, Wine path (Linux), Wine prefix (Linux), host X
- Save button persists all config changes to TOML file
- Back button returns to main view
- Linux-specific settings (Wine path/prefix) only shown on Linux builds
  </done>
</task>

<task type="auto">
  <name>Task 2: Add bot name field, settings navigation, Steam Deck fullscreen, verify CLI</name>
  <files>
    internal/gui/screens/main.go
    internal/gui/app.go
  </files>
  <action>
1. Update `internal/gui/screens/main.go`:
   - Add bot name section to main view (per Windows launcher feature parity):
     a. "Bot Name" label + `widget.NewEntry()` with placeholder "Set bot name (supporters only)"
     b. "Set" button next to the entry
     c. On click: if a gateway endpoint for bot name exists, call it. Since the exact API endpoint is unknown (noted in research open questions), implement as a placeholder that shows `dialog.ShowInformation("Bot Name", "Bot name feature coming soon.", w)`. Include a TODO comment with: `// TODO: Implement bot name API call when gateway endpoint is documented`
     d. Place this in a subsection labeled "Supporter Features" or similar
   - Update the "Settings" button `onSettings` callback to accept and use the navigation function

2. Update `internal/gui/app.go`:
   - Add `showSettingsView(w, cfg, username, password)` function:
     - Creates settings view via `screens.MakeSettingsView(w, cfg, onBack)`
     - `onBack`: transitions back to main view via `showMainView(w, cfg, username, password)`
     - Sets window content: `w.SetContent(settingsView)`
   - Wire the Settings button in main view to call `showSettingsView`
   - Ensure Steam Deck fullscreen is properly implemented:
     - The detection should already be in place from plan 01 (in `Run()` function)
     - Verify the detection works: check `/sys/devices/virtual/dmi/id/board_vendor` for "Valve" (Linux) or `SteamDeck` env var
     - If on Steam Deck: `w.SetFullScreen(true)`
     - If on desktop: `w.Resize(fyne.NewSize(480, 640))`
   - Verify all CLI subcommands still work by checking that the root command only changes behavior when no subcommand is given and the `gui` build tag is active

3. Verify CLI coexistence:
   - `cluckers launch` -- still runs CLI pipeline (not GUI)
   - `cluckers update` -- still runs CLI update (not GUI)
   - `cluckers status` -- still runs CLI status (not GUI)
   - `cluckers login` -- still runs CLI login (not GUI)
   - `cluckers logout` -- still runs CLI logout (not GUI)
   - `cluckers steam add` -- still runs CLI steam add (not GUI)
   - `cluckers --version` -- still shows version info
   - Only `cluckers` (no subcommand) opens GUI

IMPORTANT: The `gui` build tag on `root_gui.go` only sets `rootCmd.RunE` which fires when NO subcommand matches. Cobra dispatches to subcommands before RunE, so all subcommands naturally continue to work as CLI. No additional work needed beyond verifying this.
  </action>
  <verify>
1. `go build -tags gui -o /tmp/cluckers-gui ./cmd/cluckers` succeeds
2. `go vet -tags gui ./...` passes
3. `go test ./...` passes
4. Main view shows bot name field with placeholder
5. Settings button opens Settings screen
6. Settings screen -> Save persists to config file
7. Settings screen -> Back returns to main view
8. `/tmp/cluckers-gui launch --help` shows CLI help (not GUI)
9. `/tmp/cluckers-gui status` runs CLI status (not GUI)
10. `/tmp/cluckers-gui --version` shows version string
11. Steam Deck detection properly sets fullscreen (verify by checking code path, actual Deck testing is manual)
  </verify>
  <done>
- Bot name field present in main view (placeholder until API documented)
- Settings button navigates to Settings screen and back
- Steam Deck fullscreen detection and activation working
- All CLI subcommands work unchanged when invoked directly
- GUI only activates on bare `cluckers` invocation
  </done>
</task>

</tasks>

<verification>
- Settings screen renders with all config options
- Config persistence works (save and reload)
- Bot name field present in main view
- Steam Deck fullscreen code path exists
- All CLI subcommands verified working alongside GUI
</verification>

<success_criteria>
1. Settings screen exposes gateway, verbose, game dir, wine path/prefix, hostx
2. Config changes persist to TOML file
3. Bot name field in main view (placeholder for now)
4. Steam Deck fullscreen detection implemented
5. All 6 CLI subcommands + version flag work unchanged
6. GUI only activates when no subcommand given
</success_criteria>

<output>
After completion, create `.planning/phases/04-cross-platform-gui/04-04-SUMMARY.md`
</output>
